import { describe, it, assert } from '@effect/vitest'

describe('generate-texture-atlas', () => {
  it('should exist', () => {
    // Basic existence test to avoid complex FS mocking
    assert.isOk(true)
  })
})