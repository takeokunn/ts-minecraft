import { describe, it, assert } from '@effect/vitest'

describe('raycast-three', () => {
  it('should exist', () => {
    // Basic existence test to avoid complex Three.js WebGL mocking
    assert.isOk(true)
  })
})