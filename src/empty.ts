// Runtime stub for dev-only packages (vitest, fast-check, chai, etc.)
// These are aliased to this file in vite.config.ts to prevent them from
// being bundled into the production/dev game runtime.
export {}
