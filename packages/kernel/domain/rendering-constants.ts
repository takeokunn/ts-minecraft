/**
 * Maximum half-extent (in world units) for the shadow camera frustum.
 * Also used as the shadow-culling distance threshold (SHADOW_CULL_DIST_SQ = MAX_SHADOW_HALF_EXTENT²).
 * Value is half of the shadow camera far plane (272 / 2 = 136).
 */
export const MAX_SHADOW_HALF_EXTENT = 136
